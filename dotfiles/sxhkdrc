#
# wm independent hotkeys
#

# terminal emulator
super + Return
	roxterm

# program launcher
super + d
	rofi -show run

super + shift + d
	rofi -show drun

super + x
	~/.files/bin/fancy_lock

super + p
	passmenu

super + alt + s
	teiler

# make sxhkd reload its configuration files:
super + Escape
	pkill -USR1 -x sxhkd

super + alt + m
	mirrorscreen

# function keys
XF86AudioMute
	amixer sset Master toggle

XF86Audio{Lower,Raise}Volume
	amixer set Master 5%{-,+}
